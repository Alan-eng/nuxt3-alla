{
	"name": "nuxt-app",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev",
		"generate": "nuxt generate",
		"github_pages": "npx nuxt build --preset github_pages",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare"
	},
	"dependencies": {
		"nuxt": "^3.13.0",
		"swiper": "^11.1.10",
		"vue": "latest"
	}
}
